# Supabase
SUPABASE_URL=your-supabase-project-url
SUPABASE_KEY=your-supabase-service-role-key

# Temporal
# Local dev: localhost:7233
# Production (Docker): temporal:7233
TEMPORAL_ADDRESS=localhost:7233
TEMPORAL_NAMESPACE=default

# Temporal UI URL (for proxying through backend)
# Local dev: http://localhost:8233
# Production (Docker): http://temporal-ui:8080
TEMPORAL_UI_URL=http://localhost:8233

# Server
PORT=3001
# Set to 'development' for local, 'production' for deployed
# This determines which ElevenLabs webhook secret is used
NODE_ENV=development

# Session secret for Temporal UI authentication
# Generate a random secret for production: openssl rand -hex 32
SESSION_SECRET=change-this-in-production

# Frontend URL (for CORS)
# Local dev: http://localhost:3000
# Production (Docker): https://your-domain.com
FRONTEND_URL=http://localhost:3000

# ElevenLabs Webhook Secret
# Each webhook URL in ElevenLabs generates its own secret
# Local dev (ngrok): Use secret from ngrok webhook URL
# Production: Use secret from production webhook URL (https://your-domain.com/api/webhooks/elevenlabs/conversation)
ELEVENLABS_WEBHOOK_SECRET=
ELEVENLABS_INBOUND_WEBHOOK_SECRET=